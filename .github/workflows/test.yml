on: [push, pull_request]
name: Test
jobs:
  testWithTox:
    name: Test with tox
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: njzjz/tox@gh-action
    - uses: codecov/codecov-action@v3
